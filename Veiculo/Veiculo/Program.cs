
namespace Veiculo {
    class Program {
        static void Main(string[] args) {
            Veiculo veiculo = null;
            Menu menu = new Menu();
            menu.menu(veiculo);
        }
    }
}
